import { StyleSheet, Text, View } from 'react-native';

export default function PuntoEntregaForm() {
  return (
    <View style={styles.container}>
      <Text>Formulario punto de entrega</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1 },
});

import { StyleSheet, Text, View } from 'react-native';

export default function PuntoEntregaList() {
  return (
    <View style={styles.container}>
      <Text>Lista de puntos de entrega</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1 },
});

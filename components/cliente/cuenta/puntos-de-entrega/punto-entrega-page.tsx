import { StyleSheet, Text, View } from 'react-native';

export default function PuntoEntregaPage() {
  return (
    <View style={styles.container}>
      <Text>Puntos de entrega — próximamente</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1 },
});
